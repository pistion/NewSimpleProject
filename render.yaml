# Render Blueprint — full Glondia Sites deployment.
# Deploy at: https://dashboard.render.com/blueprints → New Blueprint Instance
# After first deploy, go to each service's Environment tab to fill in sync:false values.
# Docs: https://render.com/docs/blueprint-spec

services:

  # ── Backend API (NestJS) ────────────────────────────────────────────────────
  - type: web
    name: glondia-backend
    runtime: node
    region: oregon
    plan: starter
    rootDir: backend
    buildCommand: npm ci && npx prisma generate && npm run build
    startCommand: sh start.sh
    healthCheckPath: /api/v1/health

    # ── Persistent SSD ────────────────────────────────────────────────────────
    # Survives deploys and restarts. Stores the npm package cache (avoids
    # re-downloading packages on every user build) and general app data.
    # Minimum 1 GB on Starter; increase in the Render dashboard as needed.
    disk:
      name: glondia-disk
      mountPath: /var/glondia
      sizeGB: 10

    envVars:
      - key: NODE_ENV
        value: production
      # Render assigns port 10000 to web services
      - key: APP_PORT
        value: 10000
      - key: APP_NAME
        value: glondia-backend

      - key: APP_URL
        value: https://glondia-backend.onrender.com
      - key: API_BASE_URL
        value: https://glondia-backend.onrender.com/api/v1
      # Primary custom domain — both apex and www are allowed
      - key: FRONTEND_URL
        value: https://www.glondiasites.com
      - key: CORS_ORIGINS
        value: https://www.glondiasites.com,https://glondiasites.com,https://glondiasites-vrjm.onrender.com

      # ── Database (auto-linked from Render Postgres) ─────────────────────
      - key: DATABASE_URL
        fromDatabase:
          name: glondia-db
          property: connectionString

      # ── Redis (auto-linked from Render Redis) ───────────────────────────
      - key: REDIS_URL
        fromService:
          type: redis
          name: glondia-redis
          property: connectionString

      # ── Auth secrets — Render auto-generates secure random values ────────
      - key: JWT_ACCESS_SECRET
        generateValue: true
      - key: JWT_REFRESH_SECRET
        generateValue: true
      - key: FIELD_ENCRYPTION_KEY
        generateValue: true
      - key: ACCESS_TOKEN_TTL_MINUTES
        value: 15
      - key: REFRESH_TOKEN_TTL_DAYS
        value: 30

      # ── GitHub OAuth + webhooks ──────────────────────────────────────────────
      - key: GITHUB_CLIENT_ID
        sync: false
      - key: GITHUB_CLIENT_SECRET
        sync: false
      - key: GITHUB_REDIRECT_URI
        value: https://glondia-backend.onrender.com/api/v1/github/callback
      - key: GITHUB_WEBHOOK_SECRET
        sync: false
      - key: GITHUB_TOKEN
        sync: false

      # ── Render integration ───────────────────────────────────────────────
      # Set manually in Render dashboard after deploy
      - key: RENDER_API_KEY
        sync: false
      - key: RENDER_API_BASE_URL
        value: https://api.render.com/v1

      # ── Spaceship registrar ──────────────────────────────────────────────
      - key: SPACESHIP_API_KEY
        sync: false
      - key: SPACESHIP_API_SECRET
        sync: false

      # ── Email ─────────────────────────────────────────────────────────────
      - key: EMAIL_PROVIDER
        value: log
      - key: EMAIL_FROM
        value: no-reply@glondia.app
      - key: RESEND_API_KEY
        sync: false

      # ── Stripe ────────────────────────────────────────────────────────────
      - key: STRIPE_SECRET_KEY
        sync: false
      - key: STRIPE_WEBHOOK_SECRET
        sync: false

      # ── PayPal ────────────────────────────────────────────────────────────
      - key: PAYPAL_CLIENT_ID
        sync: false
      - key: PAYPAL_CLIENT_SECRET
        sync: false
      - key: PAYPAL_WEBHOOK_ID
        sync: false
      - key: PAYPAL_SANDBOX
        value: "false"

      # ── S3 object storage ─────────────────────────────────────────────────
      - key: S3_ENDPOINT
        value: https://s3.amazonaws.com
      - key: S3_REGION
        value: ap-southeast-2
      - key: S3_ACCESS_KEY_ID
        sync: false
      - key: S3_SECRET_ACCESS_KEY
        sync: false
      - key: S3_ASSETS_BUCKET
        value: glondia-assets
      - key: S3_ARTIFACTS_BUCKET
        value: glondia-artifacts
      - key: S3_PUBLIC_URL
        sync: false

      # ── Persistent disk paths ─────────────────────────────────────────────
      # DATA_DIR  — general purpose persistent storage (uploads, etc.)
      # NPM_CONFIG_CACHE — npm reuses this across user project builds,
      #   dramatically cutting install time for packages already seen before.
      # BUILD_TEMP_DIR — where the deployment worker clones repos and runs
      #   builds; on the persistent disk so interrupted jobs don't leave
      #   orphaned files on ephemeral storage.
      - key: DATA_DIR
        value: /var/glondia/data
      - key: NPM_CONFIG_CACHE
        value: /var/glondia/.npm-cache
      - key: BUILD_TEMP_DIR
        value: /var/glondia/tmp

      # ── Rate limiting ─────────────────────────────────────────────────────
      - key: RATE_LIMIT_TTL_SECONDS
        value: 60
      - key: RATE_LIMIT_MAX_REQUESTS
        value: 200
      - key: LOG_LEVEL
        value: info

  # ── Frontend (React/Vite static site) ───────────────────────────────────────
  - type: web
    name: glondiasites
    runtime: static
    region: oregon
    plan: starter
    buildCommand: npm ci && npm run build
    staticPublishPath: ./dist
    routes:
      # SPA fallback — all routes serve index.html
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      # Points the React app at the deployed backend API
      - key: VITE_API_BASE_URL
        value: https://glondia-backend.onrender.com/api/v1
      # Custom domain — set in Render dashboard → glondiasites → Custom Domains
      # www.glondiasites.com  CNAME  glondiasites-vrjm.onrender.com
      # glondiasites.com      A      216.24.57.1

  # ── Redis ────────────────────────────────────────────────────────────────────
  - type: redis
    name: glondia-redis
    region: oregon
    plan: starter
    maxmemoryPolicy: noeviction

# ── PostgreSQL ───────────────────────────────────────────────────────────────────
databases:
  - name: glondia-db
    region: oregon
    plan: starter
    databaseName: glondia
    user: glondia
