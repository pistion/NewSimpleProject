# Render Sync Retention Notes

Glondiasites hosting records should remain visible when provider builds fail or services are suspended/missing. Only a user-initiated Glondiasites removal should hide a record from the active dashboard.
