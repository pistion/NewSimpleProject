{
  "name": "glondiasites",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:render": "npm ci && npm --prefix backend ci --cache /tmp/.npm-cache && npm --prefix backend run prisma:generate && npm run build && npm --prefix backend run build",
    "start": "node server/render-single-service.mjs",
    "preview": "vite preview",
    "backend:dev": "npm --prefix backend run start:dev",
    "backend:build": "npm --prefix backend run build",
    "backend:test": "npm --prefix backend run test"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.1",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.2.1",
    "vite": "^5.2.0"
  }
}
