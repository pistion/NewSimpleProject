# Start Here — Claude Code

Execute the full implementation specified in this folder against `pistion/NewSimpleProject`.

Begin with `01_CLAUDE_CODE_MASTER_PROMPT.md` and treat all files as binding requirements.

Do not stop after analysis. Inspect the repository, create a safe branch, establish baseline tests, then implement phase by phase.

Core outcome:

```text
One database-backed BuilderProject lifecycle
+ immutable revisions
+ authenticated/quota-controlled AI
+ isolated previews
+ hardened ZIP/GitHub source handling
+ durable jobs
+ idempotent Hosting handoff
+ durable billing/reconciliation
+ unified frontend
+ complete tests and rollout
```

Preserve the existing Template AI Engine and Hosting Deploy Engine. Remove parallel business implementations rather than creating another one.

Do not claim completion until `13_ACCEPTANCE_AND_DEFINITION_OF_DONE.md` passes and `14_FINAL_IMPLEMENTATION_REPORT_TEMPLATE.md` is completed with evidence.
